
start the display program after this

 command line options:

add "--" prefix

example:

./pacman  --width 1500 --height 780 --arena_picture pacman-back.png --overlay_picture  pacman-back-overlay2.png

partial words may work:

./pacman  --widt 1500 --heig 780 --arena_pictu pacman-back.png --overlay_pic  pacman-back-overlay2.png

   "width" ( 1920 )
     "height" ( 1080 )

     "no-faulty-missiles"
     "nofaultymissiles"
     "scalefactor" 
    "no-trail"
    "notrail"
    "trail"

     "pacx" ( 1234 )
     "pacy" ( 1234 )

     "ghostx" ( 1234 )
     "ghosty" ( 1234 )

     "arena_picture" ( x.png )  layout of game arena
     "overlay_picture" ( x-overlay.png ) can do without, but makes look nicer
     "configfile" ( x.ini )

     "ghosts" ( 150 )

     "drivemode"

     "colorname" ( yellow  ) ( "#ffdd00" ) Color of character. Need to use quotation marks around html color codes.
    "pacangle" ( 90 ) front angle of character
    "openglon"
    "opengloff"

    "fullscreen"
    "ghostshide"   Ghosts visible only if character or mine has clear line of sight and close enough
    "ghosts_hide"

